{
  "Quick DOM Selectors": {
    "prefix": "selectors-",
    "body": [
      "const $ = (selector) => document.querySelector(selector);",
      "const $$ = (selector) => document.querySelectorAll(selector);",
      "$0"
    ],
    "description": "Quick DOM selector helpers"
  },

  "Client Side Authentication": {
    "prefix": "authentication-",
    "body": [
      "const users = [",
      "  { id: 1, username: \"john\", password: \"1234\", role: \"student\", isLocked: false, failedAttempts: 0 },",
      "  { id: 2, username: \"admin\", password: \"admin123\", role: \"admin\", isLocked: false, failedAttempts: 0 }",
      "];",
      "",
      "const getStoredUser = () => JSON.parse(localStorage.getItem(\"sessionUser\"));",
      "const saveSession = (user) => localStorage.setItem(\"sessionUser\", JSON.stringify(user));",
      "const clearSession = () => localStorage.removeItem(\"sessionUser\");",
      "",
      "const authenticate = (username, password) => {",
      "  const user = users.find(u => u.username === username);",
      "  if (!user) return \"INVALID_USER\";",
      "  if (user.isLocked) return \"LOCKED\";",
      "",
      "  if (user.password === password) {",
      "    user.failedAttempts = 0;",
      "    saveSession(user);",
      "    return \"SUCCESS\";",
      "  }",
      "",
      "  user.failedAttempts++;",
      "  if (user.failedAttempts >= 3) user.isLocked = true;",
      "  return \"INVALID_PASSWORD\";",
      "};",
      "$0"
    ],
    "description": "Client-side authentication with session"
  },

  "Student Performance Analyzer": {
    "prefix": "studentanalyzer-",
    "body": [
      "const students = [];",
      "",
      "const calculateGrade = (marks) => {",
      "  if (marks.some(m => m < 40)) return \"Fail\";",
      "  const avg = marks.reduce((a, b) => a + b, 0) / marks.length;",
      "  if (avg >= 90) return \"O\";",
      "  if (avg >= 80) return \"A+\";",
      "  if (avg >= 70) return \"A\";",
      "  if (avg >= 60) return \"B\";",
      "  return \"C\";",
      "};",
      "",
      "const addStudent = (name, marks) => {",
      "  if (marks.some(m => m < 0 || m > 100)) return;",
      "  const total = marks.reduce((a, b) => a + b, 0);",
      "  students.push({ name, marks, total, grade: calculateGrade(marks) });",
      "};",
      "$0"
    ],
    "description": "Student grading & ranking system"
  },

  "Todo Manager": {
    "prefix": "todolist-",
    "body": [
      "let tasks = JSON.parse(localStorage.getItem(\"tasks\")) || [];",
      "",
      "const saveTasks = () => localStorage.setItem(\"tasks\", JSON.stringify(tasks));",
      "",
      "const addTask = (title) => {",
      "  tasks.push({ id: Date.now(), title, isCompleted: false, createdAt: new Date() });",
      "  saveTasks();",
      "};",
      "",
      "const toggleTask = (id) => {",
      "  tasks = tasks.map(t => t.id === id ? { ...t, isCompleted: !t.isCompleted } : t);",
      "  saveTasks();",
      "};",
      "",
      "const deleteTask = (id) => {",
      "  tasks = tasks.filter(t => t.id !== id);",
      "  saveTasks();",
      "};",
      "$0"
    ],
    "description": "Todo manager with persistence"
  },

  "Theme Toggle": {
    "prefix": "themetoggle-",
    "body": [
      "const setTheme = (theme) => {",
      "  document.body.dataset.theme = theme;",
      "  localStorage.setItem(\"theme\", theme);",
      "};",
      "",
      "const loadTheme = () => setTheme(localStorage.getItem(\"theme\") || \"light\");",
      "$0"
    ],
    "description": "Dark / Light theme toggle"
  },

  "Clock and Timer": {
    "prefix": "clocktimer-",
    "body": [
      "let timerInterval = null;",
      "",
      "const startClock = () => {",
      "  setInterval(() => {",
      "    console.log(new Date().toLocaleTimeString());",
      "  }, 1000);",
      "};",
      "",
      "const startTimer = (seconds) => {",
      "  if (timerInterval) return;",
      "  timerInterval = setInterval(() => {",
      "    if (seconds <= 0) {",
      "      clearInterval(timerInterval);",
      "      timerInterval = null;",
      "      alert(\"Time's Up!\");",
      "    } else {",
      "      seconds--;",
      "      console.log(new Date(seconds * 1000).toISOString().substr(11, 8));",
      "    }",
      "  }, 1000);",
      "};",
      "$0"
    ],
    "description": "Digital clock and countdown timer"
  },

  "Api Fetch With Retry": {
    "prefix": "apifetch-",
    "body": [
      "const API_URL = \"https://jsonplaceholder.typicode.com/posts/1\";",
      "",
      "const fetchWithRetry = async (retryCount = 1) => {",
      "  try {",
      "    const response = await fetch(API_URL);",
      "    if (!response.ok) throw new Error(\"API Error\");",
      "    const data = await response.json();",
      "    console.log(data);",
      "    return data;",
      "  } catch (err) {",
      "    if (retryCount < 3) return fetchWithRetry(retryCount + 1);",
      "    console.error(\"Failed to fetch\");",
      "  }",
      "};",
      "$0"
    ],
    "description": "API fetch with retry logic"
  },

  "Image File Upload Preview": {
    "prefix": "imagepreview-",
    "body": [
      "const fileInput = document.getElementById(\"fileInput\");",
      "const preview = document.getElementById(\"preview\");",
      "",
      "fileInput.addEventListener(\"change\", function () {",
      "  const file = this.files[0];",
      "  if (file) {",
      "    const reader = new FileReader();",
      "    reader.onload = () => preview.src = reader.result;",
      "    reader.readAsDataURL(file);",
      "  }",
      "});",
      "$0"
    ],
    "description": "Image file upload preview"
  },
  "Hello World": {
    "prefix": "js-hello",
    "body": [
      "console.log(\"Hello World\");"
    ]
  },

  "Prompt and Greet": {
    "prefix": "js-greet",
    "body": [
      "const name = prompt(\"Enter your name\");",
      "alert(\"Hello \" + name);"
    ]
  },

  "Even or Odd": {
    "prefix": "js-evenodd",
    "body": [
      "const num = Number(prompt(\"Enter a number\"));",
      "",
      "if (num % 2 === 0) {",
      "  console.log(\"Even\");",
      "} else {",
      "  console.log(\"Odd\");",
      "}"
    ]
  },

  "Largest of Two Numbers": {
    "prefix": "js-largest",
    "body": [
      "const a = Number(prompt(\"Enter A\"));",
      "const b = Number(prompt(\"Enter B\"));",
      "",
      "console.log(a > b ? a : b);"
    ]
  },

  "Grade Calculator": {
    "prefix": "js-grade",
    "body": [
      "const marks = Number(prompt(\"Enter marks\"));",
      "",
      "if (marks >= 90) console.log(\"A\");",
      "else if (marks >= 75) console.log(\"B\");",
      "else console.log(\"C\");"
    ]
  },

  "Print Numbers 1 to 10": {
    "prefix": "js-loop10",
    "body": [
      "for (let i = 1; i <= 10; i++) {",
      "  console.log(i);",
      "}"
    ]
  },

  "Sum of Array": {
    "prefix": "js-arrsum",
    "body": [
      "const arr = [10, 20, 30];",
      "let sum = 0;",
      "",
      "for (const n of arr) {",
      "  sum += n;",
      "}",
      "",
      "console.log(sum);"
    ]
  },
  "MCQ Quiz with Score Update": {
    "prefix": "mcqscore",
    "body": [
      "let score = 0;",
      "",
      "const questions = [",
      "  {",
      "    question: \"${1:Question text here}\",",
      "    options: [\"${2:Option A}\", \"${3:Option B}\", \"${4:Option C}\", \"${5:Option D}\"],",
      "    correctAnswer: ${6:0}",
      "  }",
      "];",
      "",
      "function checkAnswer(selectedIndex, questionIndex) {",
      "  if (questions[questionIndex].correctAnswer === selectedIndex) {",
      "    score++;",
      "    console.log(\"Correct! Score:\", score);",
      "  } else {",
      "    console.log(\"Wrong answer. Score:\", score);",
      "  }",
      "}",
      "",
      "// Example usage",
      "// checkAnswer(0, 0);"
    ],
    "description": "MCQ based choose correct option and update score"
  },

  "Reverse String": {
    "prefix": "js-reverse",
    "body": [
      "const str = \"hello\";",
      "let reversed = \"\";",
      "",
      "for (let i = str.length - 1; i >= 0; i--) {",
      "  reversed += str[i];",
      "}",
      "",
      "console.log(reversed);"
    ]
  },
  "Get Geolocation and Save to LocalStorage": {
    "prefix": "geolocal",
    "body": [
      "navigator.geolocation.getCurrentPosition(position => {",
      "  const latitude = position.coords.latitude;",
      "  const longitude = position.coords.longitude;",
      "",
      "  console.log(latitude, longitude);",
      "",
      "  const user = {",
      "    name: \"${1:saran}\",",
      "    latitude_pos: latitude,",
      "    longitude_pos: longitude",
      "  };",
      "",
      "  const jsonData = JSON.stringify(user);",
      "  localStorage.setItem(\"user\", jsonData);",
      "",
      "  const storedData = localStorage.getItem(\"user\");",
      "  const parsedData = JSON.parse(storedData);",
      "",
      "  console.log(parsedData);",
      "});"
    ],
    "description": "Get user geolocation and store it in localStorage"
  },

  "Count Vowels": {
    "prefix": "js-vowels",
    "body": [
      "const text = \"javascript\";",
      "let count = 0;",
      "",
      "for (const ch of text) {",
      "  if (\"aeiou\".includes(ch)) count++;",
      "}",
      "",
      "console.log(count);"
    ]
  },

  "Add Function": {
    "prefix": "js-addfn",
    "body": [
      "function add(a, b) {",
      "  return a + b;",
      "}",
      "",
      "console.log(add(5, 3));"
    ]
  },

  "Simple Object": {
    "prefix": "js-object",
    "body": [
      "const user = {",
      "  name: \"Saran\",",
      "  age: 18",
      "};",
      "",
      "console.log(user.name);"
    ]
  },

  "Object Method": {
    "prefix": "js-objmethod",
    "body": [
      "const user = {",
      "  name: \"Saran\",",
      "  greet() {",
      "    console.log(\"Hello \" + this.name);",
      "  }",
      "};",
      "",
      "user.greet();"
    ]
  },

  "DOM Text Change": {
    "prefix": "js-domtext",
    "body": [
      "document.getElementById(\"title\").innerText = \"Changed!\";"
    ]
  },

  "Button Click Counter": {
    "prefix": "js-clickcount",
    "body": [
      "let count = 0;",
      "",
      "document.getElementById(\"btn\").addEventListener(\"click\", () => {",
      "  count++;",
      "  console.log(count);",
      "});"
    ]
  },

  "Form Validation": {
    "prefix": "js-formvalidate",
    "body": [
      "form.addEventListener(\"submit\", e => {",
      "  e.preventDefault();",
      "",
      "  if (input.value === \"\") {",
      "    alert(\"Field required\");",
      "  }",
      "});"
    ]
  },

  "Fetch API": {
    "prefix": "js-fetch",
    "body": [
      "fetch(\"https://jsonplaceholder.typicode.com/users\")",
      "  .then(res => res.json())",
      "  .then(data => console.log(data))",
      "  .catch(err => console.error(err));"
    ]
  },

  "Async Await Fetch": {
    "prefix": "js-asyncfetch",
    "body": [
      "async function load() {",
      "  const res = await fetch(\"https://jsonplaceholder.typicode.com/posts\");",
      "  const data = await res.json();",
      "  console.log(data);",
      "}",
      "",
      "load();"
    ]
  },

  "Save to localStorage": {
    "prefix": "js-localsave",
    "body": [
      "const user = { name: \"Saran\", age: 18 };",
      "localStorage.setItem(\"user\", JSON.stringify(user));"
    ]
  },

  "Get from localStorage": {
    "prefix": "js-localget",
    "body": [
      "const data = JSON.parse(localStorage.getItem(\"user\"));",
      "console.log(data.name);"
    ]
  },

  "Image Upload Preview": {
    "prefix": "js-imgpreview",
    "body": [
      "input.addEventListener(\"change\", () => {",
      "  const file = input.files[0];",
      "  if (file) {",
      "    img.src = URL.createObjectURL(file);",
      "  }",
      "});"
    ]
  },
  "DOM Live Character Counter": {
    "prefix": "dom-charcount",
    "body": [
      "const textarea = document.querySelector(\"#text\");",
      "const counter = document.querySelector(\"#count\");",
      "",
      "textarea.addEventListener(\"input\", () => {",
      "  counter.innerText = textarea.value.length;",
      "});"
    ]
  },

  "DOM Dark Mode Toggle": {
    "prefix": "dom-darkmode",
    "body": [
      "const btn = document.querySelector(\"#toggle\");",
      "",
      "btn.addEventListener(\"click\", () => {",
      "  document.body.classList.toggle(\"dark\");",
      "});"
    ]
  },

  "DOM Dynamic List Add Remove": {
    "prefix": "dom-list",
    "body": [
      "const input = document.querySelector(\"#item\");",
      "const list = document.querySelector(\"#list\");",
      "",
      "function addItem() {",
      "  const li = document.createElement(\"li\");",
      "  li.innerText = input.value;",
      "",
      "  li.addEventListener(\"click\", () => li.remove());",
      "",
      "  list.appendChild(li);",
      "  input.value = \"\";",
      "}"
    ]
  },

  "DOM Search Filter": {
    "prefix": "dom-search",
    "body": [
      "const search = document.querySelector(\"#search\");",
      "const items = document.querySelectorAll(\"li\");",
      "",
      "search.addEventListener(\"keyup\", () => {",
      "  items.forEach(item => {",
      "    item.style.display =",
      "      item.innerText.toLowerCase().includes(search.value.toLowerCase())",
      "        ? \"block\"",
      "        : \"none\";",
      "  });",
      "});"
    ]
  },

  "DOM Form Validation": {
    "prefix": "dom-form",
    "body": [
      "form.addEventListener(\"submit\", e => {",
      "  e.preventDefault();",
      "  if (email.value === \"\") {",
      "    error.innerText = \"Email required\";",
      "  }",
      "});"
    ]
  },

  "DOM Modal Toggle": {
    "prefix": "dom-modal",
    "body": [
      "openBtn.onclick = () => modal.style.display = \"block\";",
      "closeBtn.onclick = () => modal.style.display = \"none\";"
    ]
  },

  "DOM Image Gallery": {
    "prefix": "dom-gallery",
    "body": [
      "thumbnails.forEach(img => {",
      "  img.addEventListener(\"click\", () => {",
      "    mainImg.src = img.src;",
      "  });",
      "});"
    ]
  },

  "DOM Accordion": {
    "prefix": "dom-accordion",
    "body": [
      "headers.forEach(header => {",
      "  header.addEventListener(\"click\", () => {",
      "    header.nextElementSibling.classList.toggle(\"show\");",
      "  });",
      "});"
    ]
  },

  "DOM Auto Counter": {
    "prefix": "dom-autocounter",
    "body": [
      "let count = 0;",
      "",
      "setInterval(() => {",
      "  count++;",
      "  counter.innerText = count;",
      "}, 1000);"
    ]
  },

  "DOM Drag and Drop": {
    "prefix": "dom-drag",
    "body": [
      "dragItem.addEventListener(\"dragstart\", e => {",
      "  e.dataTransfer.setData(\"text\", e.target.id);",
      "});",
      "",
      "dropZone.addEventListener(\"drop\", e => {",
      "  const id = e.dataTransfer.getData(\"text\");",
      "  dropZone.appendChild(document.getElementById(id));",
      "});"
    ]
  },

  "DOM Password Toggle": {
    "prefix": "dom-password",
    "body": [
      "toggle.onclick = () => {",
      "  password.type = password.type === \"password\" ? \"text\" : \"password\";",
      "};"
    ]
  },

  "DOM Scroll Progress": {
    "prefix": "dom-scroll",
    "body": [
      "window.addEventListener(\"scroll\", () => {",
      "  const scrollPercent =",
      "    (window.scrollY / (document.body.scrollHeight - window.innerHeight)) * 100;",
      "",
      "  progress.style.width = scrollPercent + \"%\";",
      "});"
    ]
  },

  "DOM Todo Complete": {
    "prefix": "dom-todo",
    "body": [
      "li.addEventListener(\"click\", () => {",
      "  li.classList.toggle(\"completed\");",
      "});"
    ]
  },

  "DOM Add Table Row": {
    "prefix": "dom-table",
    "body": [
      "function addRow() {",
      "  const row = table.insertRow();",
      "  row.insertCell(0).innerText = nameInput.value;",
      "}"
    ]
  },

  "DOM Tabs System": {
    "prefix": "dom-tabs",
    "body": [
      "tabs.forEach(tab => {",
      "  tab.addEventListener(\"click\", () => {",
      "    contents.forEach(c => c.classList.remove(\"active\"));",
      "    document.querySelector(tab.dataset.target).classList.add(\"active\");",
      "  });",
      "});"
    ]
  },

  "DOM Character Limit Warning": {
    "prefix": "dom-limit",
    "body": [
      "input.addEventListener(\"input\", () => {",
      "  if (input.value.length > 10) {",
      "    input.style.borderColor = \"red\";",
      "  }",
      "});"
    ]
  },

  "DOM Real Time Clock": {
    "prefix": "dom-clock",
    "body": [
      "setInterval(() => {",
      "  const now = new Date();",
      "  clock.innerText = now.toLocaleTimeString();",
      "}, 1000);"
    ]
  },

  "DOM Image Preview Upload": {
    "prefix": "dom-preview",
    "body": [
      "fileInput.addEventListener(\"change\", () => {",
      "  const file = fileInput.files[0];",
      "  if (file) {",
      "    preview.src = URL.createObjectURL(file);",
      "  }",
      "});"
    ]
  },

  "DOM Event Delegation": {
    "prefix": "dom-delegate",
    "body": [
      "list.addEventListener(\"click\", e => {",
      "  if (e.target.tagName === \"LI\") {",
      "    e.target.remove();",
      "  }",
      "});"
    ]
  },

  "DOM Copy to Clipboard": {
    "prefix": "dom-copy",
    "body": [
      "button.addEventListener(\"click\", () => {",
      "  navigator.clipboard.writeText(text.innerText);",
      "});"
    ]
  },
  "1. MCQ Quiz with Score": {
    "prefix": "-quizScore",
    "body": [
      "let score = 0;",
      "const quiz = [",
      " { question: \"Q1\", options: [\"A\",\"B\",\"C\",\"D\"], answer: 1 }",
      "];",
      "",
      "function selectAnswer(qIndex, optIndex) {",
      " if (quiz[qIndex].answer === optIndex) score++;",
      "}",
      "",
      "function showResult() {",
      " console.log(`Final Score: ${score}`);",
      "}"
    ],
    "description": "MCQ based quiz and score system"
  },

  "2. Shopping Cart": {
    "prefix": "-shoppingCart",
    "body": [
      "let cart = [];",
      "",
      "function addItem(item, price) {",
      " cart.push({ item, price });",
      "}",
      "",
      "function totalAmount() {",
      " return cart.reduce((sum, i) => sum + i.price, 0);",
      "}"
    ],
    "description": "Shopping cart add & total logic"
  },

  "3. Todo App Logic": {
    "prefix": "-todoLogic",
    "body": [
      "let todos = [];",
      "",
      "function addTodo(task) {",
      " todos.push({ task, done: false });",
      "}",
      "",
      "function toggleTodo(index) {",
      " todos[index].done = !todos[index].done;",
      "}"
    ],
    "description": "Todo add & toggle status"
  },

  "4. Login System": {
    "prefix": "-loginSystem",
    "body": [
      "const user = { email: \"test@mail.com\", password: \"1234\" };",
      "",
      "function login(email, password) {",
      " return email === user.email && password === user.password;",
      "}"
    ],
    "description": "Simple login validation"
  },

  "5. Timer Quiz": {
    "prefix": "-timerQuiz",
    "body": [
      "let time = 10;",
      "",
      "const timer = setInterval(() => {",
      " time--;",
      " if (time === 0) clearInterval(timer);",
      "}, 1000);"
    ],
    "description": "Timer-based quiz logic"
  },

  "6. Pagination": {
    "prefix": "-pagination",
    "body": [
      "function paginate(data, page, limit) {",
      " const start = (page - 1) * limit;",
      " return data.slice(start, start + limit);",
      "}"
    ],
    "description": "Pagination logic"
  },

  "7. Form Validation": {
    "prefix": "-formValidate",
    "body": [
      "function validateForm(email, password) {",
      " return email.includes('@') && password.length >= 6;",
      "}"
    ],
    "description": "Basic form validation"
  },

  "8. Product Search": {
    "prefix": "-searchProduct",
    "body": [
      "function search(products, keyword) {",
      " return products.filter(p => p.name.includes(keyword));",
      "}"
    ],
    "description": "Search products by name"
  },

  "9. Voting System": {
    "prefix": "-votingSystem",
    "body": [
      "let votes = {};",
      "",
      "function vote(option) {",
      " votes[option] = (votes[option] || 0) + 1;",
      "}"
    ],
    "description": "Voting system logic"
  },

  "10. Expense Tracker": {
    "prefix": "-expenseTracker",
    "body": [
      "let expenses = [];",
      "",
      "function addExpense(title, amount) {",
      " expenses.push({ title, amount });",
      "}",
      "",
      "function totalExpense() {",
      " return expenses.reduce((t,e)=>t+e.amount,0);",
      "}"
    ],
    "description": "Expense tracker logic"
  },

  "11. Password Strength": {
    "prefix": "-passwordStrength",
    "body": [
      "function strength(pwd) {",
      " return pwd.length >= 8 ? 'Strong' : 'Weak';",
      "}"
    ],
    "description": "Password strength checker"
  },

  "12. Notification System": {
    "prefix": "-notifySystem",
    "body": [
      "function notify(msg) {",
      " console.log(msg);",
      " setTimeout(() => console.log('Removed'), 3000);",
      "}"
    ],
    "description": "Notification auto dismiss"
  },

  "13. Star Rating": {
    "prefix": "-starRating",
    "body": [
      "let rating = 0;",
      "",
      "function rate(star) {",
      " rating = star;",
      "}"
    ],
    "description": "Star rating logic"
  },

  "14. Quiz Review": {
    "prefix": "-quizReview",
    "body": [
      "function review(questions) {",
      " questions.forEach(q => console.log(q.question, q.answer));",
      "}"
    ],
    "description": "Quiz review system"
  },

  "15. File Upload Validation": {
    "prefix": "-fileValidate",
    "body": [
      "function validateFile(file) {",
      " return file.size < 2000000;",
      "}"
    ],
    "description": "File size validation"
  },

  "16. Leaderboard": {
    "prefix": "-leaderBoard",
    "body": [
      "function leaderboard(players) {",
      " return players.sort((a,b)=>b.score-a.score);",
      "}"
    ],
    "description": "Leaderboard sorting"
  },

  "17. Multi Step Form": {
    "prefix": "-multiStepForm",
    "body": [
      "let step = 1;",
      "",
      "function next() { step++; }",
      "function prev() { step--; }"
    ],
    "description": "Multi-step form navigation"
  },

  "18. Poll System": {
    "prefix": "-pollSystem",
    "body": [
      "let poll = {};",
      "",
      "function votePoll(opt) {",
      " poll[opt] = (poll[opt] || 0) + 1;",
      "}"
    ],
    "description": "Online poll logic"
  },

  "19. Theme Toggle": {
    "prefix": "-themeToggle",
    "body": [
      "let theme = 'light';",
      "",
      "function toggleTheme() {",
      " theme = theme === 'light' ? 'dark' : 'light';",
      "}"
    ],
    "description": "Theme toggle logic"
  },

  "20. Negative Marking Quiz": {
    "prefix": "-negativeQuiz",
    "body": [
      "let score = 0;",
      "",
      "function answer(correct) {",
      " score += correct ? 1 : -0.25;",
      "}"
    ],
    "description": "Quiz with negative marking"
  },

"21. API fetch with Retry": {
    "prefix": "-fetchwithretry",
    "body": [
      "async function fetchWithRetry(symbol, retries = 3, delay = 25000) {",
      "    const url = `https://cors-anywhere.com/https://query1.finance.yahoo.com/v8/finance/chart/${symbol}`;",
      "",
      "    for (let attempt = 1; attempt <= retries; attempt++) {",
      "        try {",
      "            const response = await fetch(url);",
      "",
      "            if (!response.ok) {",
      "                throw new Error(`HTTP Error: ${response.status}`);",
      "            }",
      "",
      "            const result = await response.json();",
      "            const price = result.chart.result[0].meta.regularMarketPrice;",
      "",
      "            console.log(`${symbol} Regular Market Price:`, price);",
      "            return;",
      "",
      "        } catch (error) {",
      "            console.error(`Attempt ${attempt} failed for ${symbol}:`, error);",
      "",
      "            if (attempt === retries) {",
      "                console.error(`All retries failed for ${symbol}`);",
      "                return;",
      "            }",
      "",
      "            await new Promise(resolve => setTimeout(resolve, delay));",
      "        }",
      "    }",
      "}"
    ],
    "description": "Fetch stock price from Yahoo Finance with retry logic"
  },

  "Inventory Manager Template": {
    "prefix": "inventory-manager",
    "body": [
      "<!DOCTYPE html>",
      "<html>",
      "<head>",
      "  <title>Inventory Manager</title>",
      "</head>",
      "<body>",
      "",
      "<h3>Inventory Manager</h3>",
      "",
      "<form id=\"form\">",
      "  Name: <input type=\"text\" id=\"name\">",
      "  SKU: <input type=\"text\" id=\"sku\">",
      "  Qty: <input type=\"number\" id=\"qty\">",
      "  Reorder: <input type=\"number\" id=\"reorder\">",
      "  <button type=\"submit\">Add</button>",
      "</form>",
      "",
      "<br>",
      "Search: <input type=\"text\" id=\"search\">",
      "<br><br>",
      "",
      "<table border=\"1\">",
      "  <thead>",
      "    <tr>",
      "      <th>Name</th>",
      "      <th>SKU</th>",
      "      <th>Qty</th>",
      "      <th>Reorder</th>",
      "      <th>Status</th>",
      "      <th>Action</th>",
      "    </tr>",
      "  </thead>",
      "  <tbody id=\"list\"></tbody>",
      "</table>",
      "",
      "<script>",
      "let items = [];",
      "",
      "const form = document.getElementById(\"form\");",
      "const list = document.getElementById(\"list\");",
      "const search = document.getElementById(\"search\");",
      "",
      "form.addEventListener(\"submit\", function(e) {",
      "  e.preventDefault();",
      "",
      "  let name = document.getElementById(\"name\").value;",
      "  let sku = document.getElementById(\"sku\").value;",
      "  let qty = parseInt(document.getElementById(\"qty\").value);",
      "  let reorder = parseInt(document.getElementById(\"reorder\").value);",
      "",
      "  if (name === \"\" || sku === \"\") {",
      "    console.log(\"Name and SKU required\");",
      "    return;",
      "  }",
      "",
      "  if (isNaN(qty) || isNaN(reorder) || qty < 0 || reorder < 0) {",
      "    console.log(\"Invalid numbers\");",
      "    return;",
      "  }",
      "",
      "  let status = qty <= reorder ? \"LOW\" : \"OK\";",
      "",
      "  items.push({ name, sku, qty, reorder, status });",
      "",
      "  showItems();",
      "  form.reset();",
      "});",
      "",
      "function showItems(data = items) {",
      "  list.innerHTML = \"\";",
      "",
      "  data.forEach((item, index) => {",
      "    list.innerHTML += `",
      "      <tr>",
      "        <td>${item.name}</td>",
      "        <td>${item.sku}</td>",
      "        <td>${item.qty}</td>",
      "        <td>${item.reorder}</td>",
      "        <td>${item.status}</td>",
      "        <td>",
      "          <button data-id=\"${index}\" class=\"update\">Update</button>",
      "          <button data-id=\"${index}\" class=\"delete\">Delete</button>",
      "        </td>",
      "      </tr>",
      "    `;",
      "  });",
      "}",
      "",
      "list.addEventListener(\"click\", function(e) {",
      "  let id = e.target.dataset.id;",
      "",
      "  if (e.target.className === \"delete\") {",
      "    items.splice(id, 1);",
      "    showItems();",
      "  }",
      "",
      "  if (e.target.className === \"update\") {",
      "    let newQty = prompt(\"Enter new quantity:\");",
      "    let qty = parseInt(newQty);",
      "",
      "    if (!isNaN(qty) && qty >= 0) {",
      "      items[id].qty = qty;",
      "      items[id].status = qty <= items[id].reorder ? \"LOW\" : \"OK\";",
      "      showItems();",
      "    } else {",
      "      console.log(\"Invalid quantity\");",
      "    }",
      "  }",
      "});",
      "",
      "search.addEventListener(\"input\", function() {",
      "  let text = search.value.toLowerCase();",
      "",
      "  let filtered = items.filter(item =>",
      "    item.name.toLowerCase().includes(text) ||",
      "    item.sku.toLowerCase().includes(text)",
      "  );",
      "",
      "  showItems(filtered);",
      "});",
      "</script>",
      "",
      "</body>",
      "</html>"
    ],
    "description": "Inventory Manager with add, update, delete, and search functionality"
  },

  "App State Backup and Restore": {
    "prefix": "app-backup",
    "body": [
      "const BACKUP_VERSION = \"1.0\";",
      "",
      "/**",
      " * Detect circular references",
      " */",
      "function hasCircularReference(obj) {",
      "  const seen = new WeakSet();",
      "",
      "  function detect(value) {",
      "    if (value && typeof value === \"object\") {",
      "      if (seen.has(value)) return true;",
      "      seen.add(value);",
      "",
      "      for (const key in value) {",
      "        if (detect(value[key])) return true;",
      "      }",
      "    }",
      "    return false;",
      "  }",
      "",
      "  return detect(obj);",
      "}",
      "",
      "/**",
      " * Validate app state structure",
      " */",
      "function validateAppState(state) {",
      "  if (!state || typeof state !== \"object\") return false;",
      "",
      "  if (!state.user || typeof state.user.name !== \"string\") return false;",
      "  if (!Array.isArray(state.settings)) return false;",
      "",
      "  return true;",
      "}",
      "",
      "/**",
      " * Create backup",
      " */",
      "function backupState(appState) {",
      "  try {",
      "    if (hasCircularReference(appState)) {",
      "      throw new Error(\"Circular reference detected\");",
      "    }",
      "",
      "    if (!validateAppState(appState)) {",
      "      throw new Error(\"Invalid app state structure\");",
      "    }",
      "",
      "    const backupPayload = {",
      "      version: BACKUP_VERSION,",
      "      timestamp: new Date().toISOString(),",
      "      state: appState",
      "    };",
      "",
      "    const json = JSON.stringify(backupPayload);",
      "",
      "    return `Backup v${BACKUP_VERSION} created`;",
      "  } catch (error) {",
      "    return `Backup failed: ${error.message}`;",
      "  }",
      "}",
      "",
      "/**",
      " * Restore backup",
      " */",
      "function restoreState(backupJSON) {",
      "  try {",
      "    const parsed = JSON.parse(backupJSON);",
      "",
      "    if (!parsed.version || !parsed.state) {",
      "      throw new Error(\"Invalid backup format\");",
      "    }",
      "",
      "    if (parsed.version !== BACKUP_VERSION) {",
      "      throw new Error(`Unsupported backup version ${parsed.version}`);",
      "    }",
      "",
      "    if (!validateAppState(parsed.state)) {",
      "      throw new Error(\"Corrupted backup data\");",
      "    }",
      "",
      "    const restoredState = parsed.state;",
      "",
      "    return \"State restored successfully\";",
      "  } catch (error) {",
      "    return `Restore failed: ${error.message}`;",
      "  }",
      "}"
    ],
    "description": "App state backup and restore utility with validation and circular reference detection"
  },

"PersistentCart Class": {
    "prefix": "persistentcart-",
    "body": [
      "class PersistentCart {",
      "  constructor() {",
      "    this.localKey = \"localCart\";",
      "    this.sessionKey = \"sessionCart\";",
      "    this.maxItems = 20;",
      "",
      "    if (!localStorage.getItem(this.localKey)) {",
      "      localStorage.setItem(this.localKey, JSON.stringify({}));",
      "    }",
      "  }",
      "",
      "  getLocalCart() {",
      "    return JSON.parse(localStorage.getItem(this.localKey)) || {};",
      "  }",
      "",
      "  saveLocalCart(cart) {",
      "    localStorage.setItem(this.localKey, JSON.stringify(cart));",
      "  }",
      "",
      "  addItem(name, qty) {",
      "    const cart = this.getLocalCart();",
      "    const totalQty = Object.values(cart).reduce((a, b) => a + b, 0);",
      "",
      "    if (totalQty + qty > this.maxItems) {",
      "      console.log(\"Cannot add items. Max limit (20) reached.\");",
      "      return;",
      "    }",
      "",
      "    cart[name] = (cart[name] || 0) + qty;",
      "    this.saveLocalCart(cart);",
      "",
      "    console.log(`${name} added. Quantity: ${cart[name]}`);",
      "  }",
      "",
      "  removeItem(name, qty) {",
      "    const cart = this.getLocalCart();",
      "",
      "    if (!cart[name]) {",
      "      console.log(\"Item not found.\");",
      "      return;",
      "    }",
      "",
      "    cart[name] -= qty;",
      "",
      "    if (cart[name] <= 0) {",
      "      delete cart[name];",
      "    }",
      "",
      "    this.saveLocalCart(cart);",
      "    console.log(`${name} removed.`);",
      "  }",
      "",
      "  syncSession() {",
      "    const localCart = this.getLocalCart();",
      "    sessionStorage.setItem(this.sessionKey, JSON.stringify(localCart));",
      "",
      "    const localCount = Object.keys(localCart).length;",
      "    const sessionCount = Object.keys(",
      "      JSON.parse(sessionStorage.getItem(this.sessionKey)) || {}",
      "    ).length;",
      "",
      "    console.log(",
      "      `Cart synced: ${localCount} items (local:${localCount}, session:${sessionCount})`",
      "    );",
      "  }",
      "",
      "  logout() {",
      "    sessionStorage.removeItem(this.sessionKey);",
      "    console.log(\"Session cart cleared.\");",
      "  }",
      "}"
    ],
    "description": "PersistentCart class using localStorage and sessionStorage"
  },

"House Blueprint with Factory": {
    "prefix": "houseBlueprint",
    "body": [
      "// 🏗️ Blueprint (Class)",
      "function House(color, rooms) {",
      "  // 🛑 Validation",
      "  if (typeof color !== 'string') {",
      "    throw new Error('Color must be a string');",
      "  }",
      "",
      "  if (typeof rooms !== 'number' || rooms <= 0) {",
      "    throw new Error('Rooms must be a number greater than 0');",
      "  }",
      "",
      "  // 🏷️ Auto-incrementing ID",
      "  this.id = House._currentId++;",
      "  ",
      "  // 🎨 Required properties",
      "  this.color = color;",
      "  this.rooms = rooms;",
      "",
      "  this.type = 'Residential';",
      "}",
      "",
      "// Static counter for unique IDs",
      "House._currentId = 1;",
      "",
      "// 🧠 Shared prototype method (NOT per instance)",
      "House.prototype.getDetails = function() {",
      "  return {",
      "    id: this.id,",
      "    color: this.color,",
      "    rooms: this.rooms,",
      "    type: this.type",
      "  };",
      "};",
      "",
      "// 🏭 Factory function",
      "function createHouseBatch(batchArray) {",
      "  if (!Array.isArray(batchArray)) {",
      "    throw new Error('Input must be an array');",
      "  }",
      "",
      "  return batchArray.map(spec => {",
      "    return new House(spec.color, spec.rooms);",
      "  });",
      "}"
    ],
    "description": "Creates a House constructor with validation, prototype method, and factory function"
  }
}


